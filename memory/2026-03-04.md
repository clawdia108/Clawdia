# 2026-03-04

## Co jsem udělal
- Rozšířil jsem `agent-control-studio` o skutečný OpenClaw runtime runner místo mock sekvence.
- `/api/run` teď spouští `openclaw agent --local --json` a ukládá detailní run reporty včetně `actualModel`, `provider`, `sessionId`, usage a raw JSON výsledku.
- Doplnil jsem report gallery, export všech reportů do JSON, detail drawer a JSON tree renderer.
- Doplnil jsem runtime bridge mapování `dealops -> pipelinepilot` a `timebox -> calendarcaptain`, protože UI/control-plane a OpenClaw runtime mají rozdílné agent ids.
- Přepsal jsem `agent-control-studio/CODEX_PLAYBOOK.md` a `agent-control-studio/README.md`, aby odpovídaly realitě.

## Co jsem ověřil
- `pnpm lint`
- `pnpm build`
- `pnpm test`
- Reálný smoke test přes `POST /api/run` s agentem `reviewer`, který doběhl do `completed` a vrátil raw JSON výsledek.

## Důležité zjištění
- Studio model selection je zatím recommendation layer.
- OpenClaw runtime stále běží na modelu nakonfigurovaném u daného runtime agenta.
- Reporty proto explicitně ukazují rozdíl mezi:
  - požadovaným route modelem
  - skutečným runtime modelem

## Co čeká na příště
- zjistit, jestli OpenClaw CLI/gateway umí skutečný per-run model override bez přepisování agent configu
- případně doplnit persistentní ukládání reportů mimo RAM
