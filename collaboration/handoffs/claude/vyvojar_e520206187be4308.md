# Claude Handoff — vyvojar

- Request: e520206187be4308
- Success: yes
- Source: vyvojar
- Result file: bus/claude-results/e520206187be4308.json
- Saved output: n/a

## Summary
Good. Here's the remediation split:

---

## CLAWDIA AUDIT REMEDIATION SPLIT

**PRINCIPLES**
- OpenClaw/Spojka is the single source of truth and control-plane owner. Everything else serves it.
- Fix real breakage first. Cosmetic alignment (naming) comes after correctness.
- No orphaned infra — if a flag/file/path isn't wired, delete or activate it.

---

**CLAUDE_OWNS** *(interactive sessions + code edits)*

1. **Naming drift** — canonicalize agent IDs across `agent-registry.json`, `EXECUTION_STATE.json`, `agent-states.json`, and `agent_dispatcher.py`. One list: `[obchodak, archivar, textar, strateg, postak, kalendar, vyvojar, hlidac, kontrolor, udrzbar, planovac, spojka]`. Purge aliases (CalendarCaptain, inboxforge, growthlab, dealops, timebox, reviewer).

2. **Model-router drift** — `model-router.json` is the only authority. `model-availability.json` becomes a read-only capability cache populated by a health probe, not manually. Direct Claude API calls in Python scripts must use `lib
